package org.joonseolee.inapp.util;

public class StringUtil {

  /**
   * Checks whether the provided String reference is non-null.
   *
   * @param value the String reference to check
   * @return `true` if the provided reference is not null, `false` otherwise
   */
  public static boolean isString(String value) {
    return value != null;
  }
}